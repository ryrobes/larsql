{
  "cascade_id": "test_impartial_rejection",
  "description": "Test case: Agent produces minimal output, impartial validator should reject it",
  "inputs_schema": {
    "task": "The detailed task to complete"
  },
  "cells": [
    {
      "name": "do_work",
      "instructions": "{{ input.task }}\n\nProvide just a very brief, minimal response (1-2 sentences max). Don't provide details or thoroughness.",
      "rules": {
        "max_turns": 1,
        "max_attempts": 3,
        "loop_until": "satisfied",
        "loop_until_silent": true
      }
    }
  ]
}