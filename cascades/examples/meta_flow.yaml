cascade_id: meta_flow
cells:
- instructions: I have a string 'hello'. I need to validate if it is uppercase. Use
    the 'validator_tool' tool.
  name: meta_step
  rules:
    max_turns: 2
  skills:
  - run_code
