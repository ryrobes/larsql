type_id: python_data
display_name: Python
icon: mdi:language-python
color: "#fbbf24"
name_prefix: python
description: "Python data transformation phase"
category: "data"
tags:
  - "beginner"
  - "quick-start"
  - "data-processing"
  - "popular"

template:
  tool: python_data
  inputs:
    code: |
      # Access prior cell outputs as DataFrames:
      # df = data.phase_name
      #
      # Set result to a DataFrame or dict:
      result = {"message": "Hello from Python"}
