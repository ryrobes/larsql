type_id: vega_chart
display_name: Chart (Vega-Lite)
icon: mdi:chart-areaspline
color: '#10b981'
name_prefix: vega
description: Create Vega-Lite charts with declarative grammar - JSON-based visualization
category: llm
tags:
- intermediate
- visualization
- chain
template:
  instructions: 'Create a Vega-Lite visualization for:

    {{ outputs.previous_phase }}


    Use the create_vega_lite tool to build a chart specification.


    Guidelines:

    - Use dark theme (dark background, light text)

    - Choose appropriate mark type (bar, line, point, area, etc.)

    - Include clear title and axis labels

    - Add tooltips for interactivity


    Describe what the chart shows after it''s rendered.

    '
  model: google/gemini-2.5-flash
  traits:
  - create_vega_lite
  rules:
    max_turns: 3
