type_id: windlass_data
display_name: LLM Cell
icon: mdi:robot
color: "#a78bfa"
name_prefix: llm_cell
description: "LLM-powered data phase - run LLM as a polyglot cell in data pipeline"
category: "data"
tags:
  - "intermediate"
  - "ai-ml"
  - "data-processing"
  - "chain"

template:
  tool: windlass_data
  inputs:
    instructions: |
      You are processing data in a polyglot pipeline.

      Input data: {{ outputs.previous_phase }}

      Task: Transform, enrich, or analyze this data.

      Return structured results (dict, list, or DataFrame).
    tackle: []
    model: google/gemini-2.5-flash
    max_turns: 3
