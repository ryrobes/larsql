# RVBBIT Docker Compose - Tiered Deployment
#
# USAGE:
#   Default (RVBBIT + ClickHouse):
#     docker compose up
#
#   Full stack (+ Elasticsearch/Kibana):
#     docker compose --profile full up
#
#   Standalone (BYOI - you provide ClickHouse):
#     RVBBIT_CLICKHOUSE_HOST=your-clickhouse.example.com docker compose up rvbbit
#
#   Scale workers:
#     docker compose up --scale rvbbit=3
#
# ENVIRONMENT:
#   Copy .env.example to .env and configure your API keys
#

services:
  # ===========================================================================
  # RVBBIT SERVER
  # ===========================================================================
  rvbbit:
    build:
      context: .
      dockerfile: Dockerfile
    image: rvbbit:latest
    ports:
      - "5001:5001"
    environment:
      # Required
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:?Set OPENROUTER_API_KEY in .env}

      # ClickHouse connection
      - RVBBIT_USE_CLICKHOUSE_SERVER=true
      - RVBBIT_CLICKHOUSE_HOST=${RVBBIT_CLICKHOUSE_HOST:-clickhouse}
      - RVBBIT_CLICKHOUSE_PORT=${RVBBIT_CLICKHOUSE_PORT:-9000}
      - RVBBIT_CLICKHOUSE_DATABASE=${RVBBIT_CLICKHOUSE_DATABASE:-rvbbit}

      # Optional integrations
      - HF_TOKEN=${HF_TOKEN:-}
      - ELEVENLABS_API_KEY=${ELEVENLABS_API_KEY:-}
      - ELEVENLABS_VOICE_ID=${ELEVENLABS_VOICE_ID:-}

      # Model configuration
      - RVBBIT_DEFAULT_MODEL=${RVBBIT_DEFAULT_MODEL:-google/gemini-2.5-flash-lite}
      - RVBBIT_PROVIDER_BASE_URL=${RVBBIT_PROVIDER_BASE_URL:-https://openrouter.ai/api/v1}
    volumes:
      - rvbbit-data:/data
      # Development: mount local cascades
      # - ./cascades:/app/cascades
      # - ./traits:/app/traits
    networks:
      - rvbbit
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M
    # Only wait for ClickHouse if using bundled instance
    depends_on:
      clickhouse:
        condition: service_healthy
        required: false

  # ===========================================================================
  # CLICKHOUSE
  # ===========================================================================
  clickhouse:
    image: clickhouse/clickhouse-server:24.3-alpine
    container_name: rvbbit-clickhouse
    ports:
      - "8123:8123"   # HTTP interface
      - "9000:9000"   # Native protocol
    environment:
      - CLICKHOUSE_DB=rvbbit
      - CLICKHOUSE_USER=default
      - CLICKHOUSE_PASSWORD=
      - CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
    volumes:
      - clickhouse-data:/var/lib/clickhouse
      - clickhouse-logs:/var/log/clickhouse-server
    networks:
      - rvbbit
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 256M
    ulimits:
      nofile:
        soft: 262144
        hard: 262144

  # ===========================================================================
  # ELASTICSEARCH (profile: full)
  # ===========================================================================
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.3
    container_name: rvbbit-elasticsearch
    profiles:
      - full
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - cluster.routing.allocation.disk.threshold_enabled=false
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    networks:
      - rvbbit
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # ===========================================================================
  # KIBANA (profile: full)
  # ===========================================================================
  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.3
    container_name: rvbbit-kibana
    profiles:
      - full
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
      - xpack.security.enabled=false
    ports:
      - "5601:5601"
    networks:
      - rvbbit
    depends_on:
      elasticsearch:
        condition: service_healthy
    restart: unless-stopped

volumes:
  rvbbit-data:
  clickhouse-data:
  clickhouse-logs:
  elasticsearch-data:

networks:
  rvbbit:
    driver: bridge
