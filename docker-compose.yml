# LARS Docker Compose - Tiered Deployment
#
# USAGE:
#   Default (LARS + ClickHouse):
#     docker compose up
#
#   Full stack (+ Elasticsearch/Kibana):
#     docker compose --profile full up
#
#   Standalone (BYOI - you provide ClickHouse):
#     LARS_CLICKHOUSE_HOST=your-clickhouse.example.com docker compose up lars
#
#   Scale workers:
#     docker compose up --scale lars=3
#
# ENVIRONMENT:
#   Copy .env.example to .env and configure your API keys
#

services:
  # ===========================================================================
  # LARS SERVER
  # ===========================================================================
  lars:
    build:
      context: .
      dockerfile: Dockerfile
    image: lars:latest
    ports:
      - "5001:5001"
    environment:
      # Required
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:?Set OPENROUTER_API_KEY in .env}

      # ClickHouse connection
      - LARS_USE_CLICKHOUSE_SERVER=true
      - LARS_CLICKHOUSE_HOST=${LARS_CLICKHOUSE_HOST:-clickhouse}
      - LARS_CLICKHOUSE_PORT=${LARS_CLICKHOUSE_PORT:-9000}
      - LARS_CLICKHOUSE_DATABASE=${LARS_CLICKHOUSE_DATABASE:-lars}

      # Optional integrations
      - HF_TOKEN=${HF_TOKEN:-}
      - ELEVENLABS_API_KEY=${ELEVENLABS_API_KEY:-}
      - ELEVENLABS_VOICE_ID=${ELEVENLABS_VOICE_ID:-}

      # Model configuration
      - LARS_DEFAULT_MODEL=${LARS_DEFAULT_MODEL:-google/gemini-2.5-flash-lite}
      - LARS_PROVIDER_BASE_URL=${LARS_PROVIDER_BASE_URL:-https://openrouter.ai/api/v1}
    volumes:
      - lars-data:/data
      # Development: mount local cascades
      # - ./cascades:/app/cascades
      # - ./skills:/app/skills
    networks:
      - lars
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M
    # Only wait for ClickHouse if using bundled instance
    depends_on:
      clickhouse:
        condition: service_healthy
        required: false

  # ===========================================================================
  # CLICKHOUSE
  # ===========================================================================
  clickhouse:
    image: clickhouse/clickhouse-server:24.3-alpine
    container_name: lars-clickhouse
    ports:
      - "8123:8123"   # HTTP interface
      - "9000:9000"   # Native protocol
    environment:
      - CLICKHOUSE_DB=lars
      - CLICKHOUSE_USER=default
      - CLICKHOUSE_PASSWORD=
      - CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
    volumes:
      - clickhouse-data:/var/lib/clickhouse
      - clickhouse-logs:/var/log/clickhouse-server
    networks:
      - lars
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 256M
    ulimits:
      nofile:
        soft: 262144
        hard: 262144

  # ===========================================================================
  # ELASTICSEARCH (profile: full)
  # ===========================================================================
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.3
    container_name: lars-elasticsearch
    profiles:
      - full
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - cluster.routing.allocation.disk.threshold_enabled=false
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    networks:
      - lars
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # ===========================================================================
  # KIBANA (profile: full)
  # ===========================================================================
  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.3
    container_name: lars-kibana
    profiles:
      - full
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
      - xpack.security.enabled=false
    ports:
      - "5601:5601"
    networks:
      - lars
    depends_on:
      elasticsearch:
        condition: service_healthy
    restart: unless-stopped

volumes:
  lars-data:
  clickhouse-data:
  clickhouse-logs:
  elasticsearch-data:

networks:
  lars:
    driver: bridge
