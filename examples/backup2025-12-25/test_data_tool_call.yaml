cascade_id: "test_data_tool_call"
description: "Test calling a data cascade as a tool from an LLM cascade"

phases:
  - name: "use_data_tool"
    instructions: |
      You are a data analyst assistant. Use the data_cascade_test tool to analyze data with a threshold of 2.

      Call the tool with threshold=2, then summarize the results in a friendly way.
    tackle:
      - "data_cascade_test"
    rules:
      max_turns: 3
