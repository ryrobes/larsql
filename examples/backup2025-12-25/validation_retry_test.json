{
  "cascade_id": "validation_retry_test",
  "description": "Tests validation retry mechanism with a strict length validator",
  "inputs_schema": {
    "topic": "The topic to write about"
  },
  "phases": [{
    "name": "write_content",
    "instructions": "Write EXACTLY 5 words about: {{ input.topic }}",
    "rules": {
      "max_turns": 1,
      "max_attempts": 3,
      "loop_until": "length_check",
      "retry_instructions": "Validation failed: {{ validation_error }}\n\nYour response was too short. Please write a longer response (at least 100 characters). This is attempt {{ attempt }}/{{ max_attempts }}."
    },
    "tackle": ["set_state"]
  }]
}
