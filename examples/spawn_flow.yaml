cascade_id: side_effect_parent
cells:
- async_cascades:
  - context_in: false
    input_map:
      text: SIDE EFFECT VALIDATION
    ref: windlass/examples/validator.json
    trigger: on_start
  instructions: I am doing important work. Count to 3. A background task is spawned
    to validate data.
  name: main_task
  rules:
    max_turns: 1
  traits: []
