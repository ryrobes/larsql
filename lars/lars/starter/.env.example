# =============================================================================
# LARS Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# Run `lars doctor` to verify your configuration.

# =============================================================================
# Workspace Root (IMPORTANT!)
# =============================================================================
# Set this to the ABSOLUTE PATH of your project directory.
# All LARS data (logs, states, session databases) is stored relative to this.
#
# Example: /home/user/my-lars-project
#
# If you run LARS from different directories without setting this,
# your data may end up scattered in unexpected locations.
#
# NOTE: `lars init` automatically sets this to an absolute path.
LARS_ROOT=.

# =============================================================================
# LLM Provider (Required)
# =============================================================================
# LARS uses OpenRouter by default for LLM access.
# Get your API key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Optional: Override the default model
# LARS_DEFAULT_MODEL=anthropic/claude-sonnet-4

# Optional: Override the provider base URL (for local models, Ollama, etc.)
# LARS_PROVIDER_BASE_URL=http://localhost:11434/v1

# =============================================================================
# ClickHouse Database (Required)
# =============================================================================
# LARS requires ClickHouse for logging, analytics, and vector search.
#
# Quick start with Docker:
#   docker run -d --name lars-clickhouse \
#     -p 9000:9000 -p 8123:8123 \
#     -e CLICKHOUSE_USER=lars \
#     -e CLICKHOUSE_PASSWORD=lars \
#     -e CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1 \
#     -v lars-clickhouse-data:/var/lib/clickhouse \
#     clickhouse/clickhouse-server:latest
#
# Then run: lars db init

LARS_CLICKHOUSE_HOST=localhost
LARS_CLICKHOUSE_PORT=9000
LARS_CLICKHOUSE_DATABASE=lars
LARS_CLICKHOUSE_USER=lars
LARS_CLICKHOUSE_PASSWORD=lars

# =============================================================================
# Optional: HuggingFace (Harbor Integration)
# =============================================================================
# Enable HuggingFace Spaces as tools via Harbor.
# Get your token at: https://huggingface.co/settings/tokens
# HF_TOKEN=hf_your-token-here

# =============================================================================
# Optional: ElevenLabs (Text-to-Speech)
# =============================================================================
# Enable the `say` tool for voice output.
# ELEVENLABS_API_KEY=your-key-here
# ELEVENLABS_VOICE_ID=your-voice-id

# =============================================================================
# Optional: Brave Search (Web Search Tool)
# =============================================================================
# Enable web search capabilities.
# Get your key at: https://brave.com/search/api/
# BRAVE_SEARCH_API_KEY=your-key-here
