# MongoDB connection (materialized into DuckDB)
# Requires: export MONGODB_URI="mongodb://root:password@localhost:27117"
# Start container: docker compose -f docker-compose.sql-tests.yml up -d mongodb
connection_name: mongodb_test
type: mongodb
mongodb_uri_env: MONGODB_URI
database: testdb
enabled: true
sample_row_limit: 100
distinct_value_threshold: 50
