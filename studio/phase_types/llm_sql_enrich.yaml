type_id: llm_sql_enrich
display_name: SQL + LLM Enrich
icon: mdi:database-plus
color: '#9333ea'
name_prefix: enrich
description: SQL query with rvbbit() for LLM-powered data enrichment
category: data
tags:
- intermediate
- ai-ml
- data-processing
- novel
- powerful
template:
  tool: sql_data
  inputs:
    query: "-- Use rvbbit() to enrich data with LLM-powered extraction\n-- Reference prior cells: SELECT * FROM _phase_name\n\
      \nSELECT\n  column_name,\n\n  -- Extract/classify/transform using LLM\n  rvbbit(\n    'Your extraction prompt here',\n\
      \    column_name\n  ) as enriched_field\n\nFROM _previous_phase\nLIMIT 10\n"
    materialize: 'true'
