cascade_id: test_mcp_integration
description: Test MCP tool integration with filesystem server

cells:
  - name: read_test_file
    instructions: |
      Read the contents of the file at /tmp/test_mcp_file.txt

      Use the appropriate tool to read the file and tell me what it says.
    skills: "manifest"  # Let Quartermaster pick the right tool
    rules:
      max_turns: 3
