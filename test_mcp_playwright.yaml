cascade_id: test_mcp_playwright
description: Test MCP Playwright integration with Quartermaster

cells:
  - name: navigate_and_screenshot
    instructions: |
      I need to take a screenshot of the RVBBIT GitHub page.

      First navigate to https://github.com/rvbbit and then take a screenshot.
      Use the appropriate browser automation tools.
    skills: "manifest"  # Let Quartermaster pick Playwright tools
    rules:
      max_turns: 5
