cascade_id: test_mcp_unique_tool
description: Test MCP tool with MCP-specific tool (read_text_file)

cells:
  - name: read_with_mcp
    instructions: |
      Read the file /tmp/test_mcp_file.txt using read_text_file tool.

      This tool is from the MCP filesystem server. Use it to read the file
      and tell me what it contains.
    skills: [read_text_file]  # Explicitly use MCP-specific tool
    rules:
      max_turns: 3
