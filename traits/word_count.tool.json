{
  "tool_id": "word_count",
  "description": "Count lines, words, and characters in files or text input.",
  "inputs_schema": {
    "file_path": "Path to file to count (optional)",
    "text": "Direct text to count (if no file_path provided)"
  },
  "type": "shell",
  "command": "{% if file_path %}wc {{ file_path }}{% else %}echo '{{ text }}' | wc{% endif %}",
  "timeout": 10,
  "sandbox": false
}
